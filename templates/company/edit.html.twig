{% extends 'base.html.twig' %}

{% block title %}Modifier l'entreprise{% endblock %}

{% block body %}
    <h1 class="title">Modifier l'entreprise</h1>
    <div class="container">

        {{ include('company/_form.html.twig', {'button_label': 'Update'}) }}

        {{ include('company/_delete_form.html.twig') }}

    </div>

    <div class="row justify-content-center">
        <a class="btn btn-outline-light col-2" href="{{ path('company_index') }}">Retour à la liste</a>
    </div>


{% endblock %}
