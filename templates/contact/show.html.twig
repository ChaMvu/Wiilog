{% extends 'base.html.twig' %}

{% block title %}Contact{% endblock %}

{% block body %}
    <h1 class="title">Informations du contact</h1>
    <div class="container">
        <div class="row">
            <table class="table">
                <tbody>
                <tr>
                    <th>Nom</th>
                    <th>Email</th>
                    <th>Entreprise</th>
                </tr>
                <tr>
                    <td>{{ contact.name }}</td>
                    <td>{{ contact.email }}</td>
                    <td>{{ contact.company.name }}</td>
                </tr>
                </tbody>
            </table>
        </div>
    </div>
    <br>


    <div class="row justify-content-center">
        <a class="btn btn-outline-light col-3" href="{{ path('contact_edit', {'id': contact.id}) }}">Modifier</a>
    </div>

    <br>

    <div class="row justify-content-center">
        <a class="btn btn-outline-light col-3" href="{{ path('contact_index') }}">Retour à la liste</a>
    </div>
    <br>
    {{ include('contact/_delete_form.html.twig') }}

{% endblock %}
