{% extends 'base.html.twig' %}

{% block title %}Modifier le contact{% endblock %}

{% block body %}
    <h1 class="title">Modifier le contact</h1>
    <div class="container">

        {{ include('contact/_form.html.twig', {'button_label': 'Modifier'}) }}


        {{ include('contact/_delete_form.html.twig') }}


        <div class="row justify-content-center">
            <a class="btn btn-outline-light col-3" href="{{ path('contact_index') }}">Retour à la liste</a>
        </div>

    </div>


{% endblock %}
