{% block header %}

    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="{{ path('home_index') }}">Home</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('company_index') }}">Entreprises</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('contact_index')}}">Contacts</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('contact_new') }}">Ajouter un contact</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('company_new') }}">Ajouter une entreprise</a>
                </li>
            </ul>
        </div>
    </nav>



{% endblock %}